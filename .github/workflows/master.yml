name: master

on:
  pull_request:
    branches:
      - master
  push:
    branches:
      - master

jobs:
  tests:
    strategy:
      matrix:
        os: [ubuntu-latest] # macos-latest, windows-latest
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v2

      - name: Install Deno
        uses: denoland/setup-deno@v1

      - name: Tests
        run: |
          cd tests
          docker-compose up -d
          cd ..
          deno test --coverage=cov -A tests
          deno coverage cov
          deno coverage --lcov cov > cov.lcov

      - name: Upload Coverage
        uses: codecov/codecov-action@v2.1.0
        with:
          name: dami
          files: cov.lcov

  linter:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Install Deno
        uses: denoland/setup-deno@v1

      - name: Lint
        run: deno lint

      - name: Formatter
        run: deno fmt --check
